{
	"name": "templates-gallery",
	"version": "0.0.0",
	"scripts": {
		"cpWeb": "cp -r ./web ./dist",
		"cpAssets": "cp -r ./assets/{img,svg}  ./dist/assets",
		"dev": "vite",
		"build": "tsc && vite build && npm run cpWeb && npm run cpAssets",
		"serve": "vite preview"
	},
	"devDependencies": {
		"@tsconfig/recommended": "^1.0.1",
		"typescript": "^4.3.2",
		"vite": "^2.6.4"
	}
}
